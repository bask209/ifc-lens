# ifc-lens

Project instructions for coding agents live in [AGENTS.md](AGENTS.md): the license and corpus rules that cannot
be relaxed, setup (including the headless-WebGL workaround), which commands to run while editing and before
finishing, the layer map, and the list of generated files that must never be hand-edited.
